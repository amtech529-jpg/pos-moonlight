class FileDownloadHelper {
  static void downloadFile(List<int> bytes, String fileName) {
    // Stub for non-web environments. Handled separately in IO logic.
    throw UnsupportedError('Only supported on Web');
  }
}
